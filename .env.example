# Supabase Configuration
# Public keys - safe to commit, used for client-side auth
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc... (anon key from Supabase dashboard)

# Server-Side Keys - NEVER commit these files
# Only add to .env.local (local development) or Vercel environment variables (production)
# KEEP THESE SECRET - They bypass all Row-Level Security policies!
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc... (service role key from Supabase dashboard)

# App Configuration
NEXT_PUBLIC_APP_NAME=Wirtschaftlichkeitsplan
NEXT_PUBLIC_APP_URL=http://localhost:3000
